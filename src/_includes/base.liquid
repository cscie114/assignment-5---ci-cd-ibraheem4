<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="This is a class website!">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/assets/styles/main.css">
  </head>
  <body class="body">
    <header>
      <h1>{{ metadata.name }}</h1>
      <h2>{{ title }}</h2>
      {% render 'navigation' %}
    </header>
    <main class="main">
      {{ content }}
    </main>
    {% render 'footer' %}
  </body>
</html>
